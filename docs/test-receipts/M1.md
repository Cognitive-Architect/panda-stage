# M1 Gate — Project Lifecycle

## 结论

- Result: **PASS**
- 工单: `B-15/45`
- 执行分支: `chore/day-15-m1-gate`
- 基线 SHA: `b47877c0d3df3eff1dba8ec6509430638314da49`
- 已验证实现 SHA: `5f28b3e737a06b099c5395404b1b957588c9d6f1`
- 结果 SHA: 以本回执所在最终交付提交为准
- Gate A: **PASS**（`docs/test-receipts/GATE-A.md`）
- GitHub Actions: **PASS**
  （[CI run 30078512820](https://github.com/Cognitive-Architect/panda-stage/actions/runs/30078512820)）

M1 项目系统满足进入 M2 的数据安全前提：没有检测到数据丢失、正式项目
损坏、recovery 自动覆盖正式项目或绝对素材路径绑死。

## Gate 前基线

在修改 Day 15 文件前，于 `origin/main`
`b47877c0d3df3eff1dba8ec6509430638314da49` 实际执行：

- `pnpm typecheck`: PASS
- `pnpm lint`: PASS
- `pnpm test:unit`: PASS（32 files / 191 tests）
- `pnpm test:integration`: PASS（4 files / 28 tests）
- `pnpm build`: PASS

## 自动化检查

| 命令 | 结果 | 实际输出 |
|---|---|---|
| `pnpm typecheck` | PASS | 应用与 Electron TypeScript 配置均通过 |
| `pnpm lint` | PASS | 0 error |
| `pnpm test:unit` | PASS | 32 files / 191 tests |
| `pnpm test:integration` | PASS | 4 files / 28 tests |
| `pnpm build` | PASS | Renderer、Main、Preload 构建成功 |
| `pnpm verify:day13` | PASS | Electron recovery UI/API/sandbox |
| `pnpm verify:day14` | PASS | Electron recent/close UI/API |
| `pnpm verify:m1` | PASS | Windows 真实完整生命周期 |
| `git diff --check` | PASS | 无空白错误 |
| Prettier | N/A | 仓库没有 Prettier 依赖或脚本；以 ESLint 和 `git diff --check` 作为格式门禁 |

GitHub Actions 在 Windows 上重新执行依赖安装、typecheck、lint、191 个
unit tests、28 个 integration tests、build、Day 13/14 Electron Gate 和
M1 lifecycle Gate，全部为 PASS。CI 已从 Day 15 起把 integration tests
纳入必跑步骤，并允许 `chore/**`、`feat/**`、`fix/**` 分支直接获得证据。

## 生命周期验收

| 项目 | 结果 | 证据 |
|---|---|---|
| Schema / Migration | PASS | Day 11 回执；models/migrations unit tests |
| 新建 / 打开 / 保存 | PASS | M1 runtime `create`、`modify-save-close-reopen` |
| 原子保存失败保护 | PASS | 注入 `EIO` 后正式文件 hash 与 `.tmp=0` |
| Autosave / Recovery | PASS | dirty recovery、模拟重启、detect/restore/ignore |
| 最近项目 | PASS | missing 保留、ID 校验 relocation、Day 14 Electron |
| 关闭三分支 | PASS | save 清理、cancel 保留、discard 安全清理 |
| Unicode 路径 | PASS | `熊猫 项目 生命周期 🐼.pandastage` |
| 项目移动重定位 | PASS | 真实目录 rename 后打开、检测 recovery、重定位 |
| 相对素材路径 | PASS | `assets/角色 图片 🐼.png`，移动后仍可读 |

### 可复现端到端操作

`pnpm verify:m1` 在真实 Windows 临时目录中顺序执行：

1. 创建 Unicode、空格和 emoji `.pandastage` 项目；
2. 写入相对素材、修改、正式保存、关闭服务并重开；
3. 在正式文件原子提交前注入 `EIO`，验证旧 hash 不变且无 `.tmp`；
4. dirty autosave 后释放进程内服务，创建新 recovery 服务模拟重启；
5. detect、restore、ignore 均不改写正式 `project.json`；
6. 真实移动项目目录，验证 recovery 和相对素材随目录继续有效；
7. 最近记录旧根为 `missing`，相同项目 ID 重定位后为 `available`；
8. close-save 清 recovery；close-cancel 保留 recovery；
   close-discard 清 recovery 且正式文件不变；
9. 损坏 JSON 打开失败且源文件字节不变。

完整操作日志：
`docs/evidence/m1/lifecycle.log`。

## 数据安全

- 是否发生数据丢失: **否**。
- 正式 `project.json` 是否被损坏: **否**。
- recovery 是否覆盖正式项目: **否**；restore 前后正式 SHA-256 均为
  `8321ef61c903e3c819cb24d39110de529c9b3931adc65267fd683463c9b7b548`。
- 原子保存故障是否保护旧文件: **是**；故障前后 SHA-256 同上，
  `.tmp` 数量为 0。
- discard 是否产生迟到 recovery: **否**；清理后 recovery 与 `.tmp`
  均为 0，`detectLatest()` 返回 `null`。
- 是否存在绝对路径绑死: **否**；项目样例只保存
  `assets/角色 图片 🐼.png`，Renderer 静态文件系统依赖匹配数为 0。

机器证据：

- `docs/evidence/m1/results.json`
- `docs/evidence/m1/lifecycle.log`
- `docs/evidence/m1/project-final.example.json`
- `docs/evidence/day-13/recovery-results.json`
- `docs/evidence/day-13/ui-results.json`
- `docs/evidence/day-14/results.json`
- `docs/evidence/day-14/ui-results.json`

## 刀刃表

| 类别 | ID | 结果 | 证据 |
|---|---|---|---|
| FUNC | FUNC-001 | PASS | 32 unit files；schema/migration |
| FUNC | FUNC-002 | PASS | create→save→close→reopen runtime |
| FUNC | FUNC-003 | PASS | autosave→restart→detect→restore |
| FUNC | FUNC-004 | PASS | Day 14 integration + Electron UI |
| CONST | CONST-001 | PASS | final project sample relative path |
| CONST | CONST-002 | PASS | injected fault + identical SHA-256 |
| CONST | CONST-003 | PASS | restore 前后正式 hash 相同 |
| CONST | CONST-004 | PASS | 下方继承债务逐条列出 |
| NEG | NEG-001 | PASS | invalid JSON read-only runtime/integration |
| NEG | NEG-002 | PASS | Windows Unicode/空格/emoji 实测 |
| NEG | NEG-003 | PASS | 真实 rename + relocation |
| NEG | NEG-004 | PASS | save fault close guard integration |
| UX | UX-001 | PASS | 稳定错误码、路径与下一步提示 |
| UX | UX-002 | PASS | Day 13/14 Electron UI 证据 |
| E2E | E2E-001 | PASS | `verify:m1` 完整顺序日志 |
| High | HIGH-001 | PASS | hash、recovery、relative path、CI 汇总 |

总计：**16/16 PASS**。

## P4 自测

| 检查点 | 结果 | 证据 |
|---|---|---|
| CF | PASS | M1 主能力全量 Gate |
| RG | PASS | Day 11～14 tests + Day 13/14 verifiers |
| NG | PASS | 损坏、EIO、Unicode、移动、关闭失败 |
| UX | PASS | recovery/recent/close Electron 证据 |
| E2E | PASS | M1 runtime operation log |
| High | PASS | 正式 hash、零 tmp、相对路径 |
| 字段完整性 | PASS | 本回执 |
| 需求映射 | PASS | 16 项刀刃表 |
| 自测执行 | PASS | Windows 真实文件和目录操作 |
| 范围边界与债务 | PASS | 无 M2 功能；债务如下 |

## 债务与未验证项

- `DEBT-TEST-B15-001`: 无。M1 高风险主路径均有自动化、真实文件系统或
  Electron 证据。
- `DEBT-PLATFORM-B15-001`: 继承 `DEBT-PLATFORM-B14-001`。Windows
  本地磁盘、Unicode、空格、emoji、slash/case/dot-segment 和 UNC
  规范化均已覆盖；没有可用的真实认证网络共享，因此不声明 UNC 网络 I/O
  已通过。此项不属于 M1 本地项目移动验收。
- `DEBT-MIGRATION-B11-001`: M0.5 probe 曾占用 `schemaVersion: 1`；
  当前仍通过 strict 形状识别兼容，旧 probe 退役后应删除兼容分支。
- `DEBT-TEST-B14-001`: 原生 MessageBox 的按钮语义和控制器分支已自动化，
  但操作系统模态框本身未做截图；Renderer recovery/recent UI 已有截图。
- `DEBT-PERF-B15-001`: Vite 报告既有主 Renderer chunk 超过 500 kB。
  这不影响 M1 数据安全、运行或构建 Gate，留待后续性能工作处理。
- Day 12、Day 13 声明的 complexity/platform/test/state/doc debt 均为无；
  未在本 Gate 中隐藏或重新定义。

## 范围审计

- 未实现素材导入、素材库 UI、角色系统、镜头编辑器或画布功能。
- 产品代码未新增功能；变更仅包括 Gate 脚本、CI、证据和架构/开发文档。
- 未创建 `docs/decisions/M1-FAILURE-REPORT.md`，因为不存在 FAIL 条件。

## 决策

**PASS：允许进入 Day 16。**
