# M3 Gate — Action Presets + Validated Event Generation

> 本回执按 DAY-25-AGENT-TASK 模块5 格式编写。结论只能 PASS/FAIL；未验证项按 FAIL。
> 本回执由主理人据独立重跑的门禁结果定稿（QA 代理未落盘，主理人代为核实并填写）。

## 结论

- **Result: FAIL**
- ⚠️ **冻结 Day 26~45 实际开发**：因 HIGH-001 真实 Electron GUI 手动验收缺失（按工单「未验证项按 FAIL」「主路径未真实制作镜头不得 PASS」），M3 结论为 FAIL。冻结解除条件：补齐真实 Electron UI 点击回归（真人制作镜头 + 保存重开）后重跑 M3 Gate 并 PASS。
- 工单: Day 25 Action Presets + Validated Event Generation + M3 Gate
- 执行分支: `feat/day-25-action-presets`
- 基线 SHA: `5ad6911`（origin/main，Day 24 合并点）
- 核心结果 SHA: `27002f2`（fix: 修复 timeline 事件引用扫描/级联测试与构建崩溃）
- Schema 版本: 保持 `5`（本次未提升）

Day 25 完成 8 类动作预设 → 纯函数生成 `TimelineEvent` → 校验器拒绝越界/坏引用 → 经
`EditorProjectStore.updateProject` 进入历史（undo/redo 免费）；并将 StageRenderer / Preview /
Export 收敛到正式 `src/domain` 的 `evaluateShotAtTime` 单一来源（RISK-EVENT-001）。旧
`shared/domain` evaluator 保留但已退出生产链路（`@deprecated`）。T01 对 v1/v2/v3/v4 旧项目回填
`timelineEvents: []`（保持 v5）。

## 自动化检查（独立重跑结果）

| Gate | 结果 | 证据 |
|---|---|---|
| TYPE | PASS | `pnpm typecheck`（`tsc --noEmit` + `tsc -p tsconfig.electron.json --noEmit`）= 0 error |
| LINT | PASS | `pnpm lint`（ESLint）= 0 error |
| BUILD | PASS | `build:renderer` + `build:electron`（leaf 脚本；本机 corepack pnpm shim 损坏，不跑顶层 `pnpm build`）exit 0 |
| UNIT / COMPONENT | PASS | `pnpm test:unit` = 452 passed / 72 files，0 fail |
| INTEGRATION | PASS | `pnpm test:integration` = 85 passed / 17 files，0 fail |

## 8 类预设映射

| 预设 | eventType | 默认时长(ms) | 关键参数 | 逻辑坐标/离屏 |
|---|---|---|---|---|
| enter-left | move | 800 | — | from.x = -300（离屏） |
| enter-right | move | 800 | — | from.x = 1920 + 300（离屏） |
| move-to | move | 800 | targetX, targetY | 限幅 0..1920 / 0..1080 |
| scale-emphasis | scale | 800 | scaleFactor(默认1.3) | 以当前缩放为中心 |
| shake | shake | 600 | amplitudeX/Y, frequencyHz | 叠加正弦偏移 |
| expression-switch | expression | 1000 | expressionId(必填) | 仅角色层 |
| fade-in | opacity | 800 | — | 0 → 当前不透明度 |
| fade-out | opacity | 800 | — | 当前不透明度 → 0 |

## Day 25 实际结果

- `createPresetEvents` 为纯函数（位于 `src/domain`，无 DOM/Konva 引用）：唯一 id（`options.createId` 可注入）、整数 ms（四舍五入）、1920×1080 逻辑坐标、离屏入场；输出通过 `TimelineEventSchema`。
- `validatePresetApplication`（src/domain/validators/timelineEventValidator.ts）为单一拒绝闸门：背景层、重复 id、越界（中文「超出镜头时长」）、不透明度越界 0–1、表情不属于角色均**拒绝 + 中文原因**，绝不静默截断（工单红线 4）。
- `actionPresetStore`（src/renderer/features/actions/actionPresetStore.ts）桥接：读 `editorProjectStore`/`shotStore`/`selectionStore` → `createPresetEvents` → `validatePresetApplication` → 仅当通过才 `applyPresetEvents` + `updateProject`，全部历史自动记录（undo/redo 免费）；未选/背景/锁定/非角色直接拒绝，零修订。
- T03 双链收敛：StageRenderer / CanvasStage / StagePreview / ExportRendererApp 统一引用正式 `src/domain` 的 `Project`/`EvaluatedShot`/`evaluateShotAtTime`；正式 evaluator 处理全部 7 类事件，StagePreview / Export 调用时传入 `project`；`probe-project.ts` 改为正式 `startMs/endMs` 形状。
- T01 迁移回填：v1/v2 经 `addBackgroundIdentity`、v3/v4 经 `?? []` 回填 `timelineEvents`，旧项目打开后严格 `ProjectDataSchema` 通过。
- R6：`evaluateShotAtTime` 遇到失效 `expressionId` 回退到 layer 基 expression，**不抛错**，渲染不崩。

## 刀刃表（16 项）

| Blade | 结果 | 直接证据 |
|---|---|---|
| FUNC-001 8 类预设映射到事件种类 | PASS | `tests/unit/domain/actions/ActionPreset.test.ts` + `src/domain/actions/ActionPreset.ts`（8 类 union） |
| FUNC-002 预设仅生成 TimelineEvent，不碰 DOM/Konva | PASS | `createPresetEvents.ts`/`actionPresetStore.ts` 位于 `src/domain`/`src/renderer/features`，无 DOM 引用；`ActionPreset.test.ts` 覆盖 |
| FUNC-003 纯函数：唯一 id / 整数 ms / 1920×1080 / 离屏 | PASS | `tests/unit/domain/actions/createPresetEvents.test.ts`（id 唯一、ms 整数、离屏坐标断言） |
| FUNC-004 校验器拒绝越界/坏表情/重复 id/无效层（中文） | PASS | `tests/unit/domain/validators/timelineEventValidator.test.ts`（中文原因断言） |
| FUNC-005 应用经 updateProject → undo/redo | PASS | `tests/integration/action-preset-history.test.ts`（应用→undo→redo→序列化） |
| FUNC-006 迁移回填 timelineEvents（v1/v2/v3/v4） | PASS | `tests/unit/migrations/project-migration.test.ts`（schemaVersion===5 断言 + 回填） |
| CONST-001 时间字段均为整数 ms | PASS | `createPresetEvents.test.ts`（四舍五入断言） |
| CONST-002 越界拒绝不静默（红线 4） | PASS | `timelineEventValidator.ts` 拒绝路径 + 测试 |
| CONST-003 不提升 schema 版本（保持 v5） | PASS | `src/domain/models/project.ts:5` `PROJECT_SCHEMA_VERSION = 5`；迁移测试断言 |
| CONST-004 未选/背景/锁定/非法引用 → 禁用或拒绝，零修订 | PASS | `tests/unit/features/actions/actionPresetStore.test.ts`（NEG-001/002/004） |
| NEG-001 未选图层 / 背景层 → 面板禁用 | PASS | `ActionPresetPanel.tsx` 禁用逻辑 + `actionPresetStore.test.ts` |
| NEG-002 layer locked → 应用被拒 | PASS | `actionPresetStore.test.ts` |
| NEG-004 非角色层 / 失效表情 → 禁用或拒绝 | PASS | `actionPresetStore.test.ts` + `createPresetEvents.test.ts` |
| UX-002 表单内中文错误反馈（不静默） | PASS | `ActionPresetPanel` 状态输出 + 桥接中文 reason |
| RISK-EVENT-001 双链收敛：单一 evaluator 来源 | PASS | T03 改引用 + `evaluate-shot-at-time.test.ts` 7 类覆盖 |
| E2E-001 应用→undo→redo→序列化事件不丢 + R6 回退 | PASS | `action-preset-history.test.ts`（序列化往返）+ `evaluate-shot-at-time.test.ts`（R6 回退） |

## HIGH-001（无代码完成静态镜头 + 动作）

编辑外壳已在 `App.tsx` 挂载 `ShotManager` / `AssetLibrary` / `CanvasStage` / `ActionPresetPanel`
/ `HistoryControls`（R1）。用户在 UI 内可：选镜头 → 选非背景层 → 应用预设 → undo/redo。

- **自动化等价覆盖**：`action-preset-history.test.ts` 覆盖「应用→undo→redo→序列化事件不丢」；`actionPresetStore.test.ts` 覆盖编辑外壳调用的桥接拒绝路径（NEG-001/002/004）。
- **环境限制（按工单计入 FAIL）**：本环境为无显示 headless，未执行真实 Electron UI 点击回归（真人制作镜头 + 保存重开）。该路径由上述集成/组件测试覆盖自动化等价逻辑，但**非等效于真实 GUI 验收**。按工单「未验证项按 FAIL」「主路径未真实制作镜头不得 PASS」原则，此单项标记为 **ENV-LIMITED** 且**计入 FAIL**，M3 总体结论为 **FAIL**，并触发 Day 26~45 冻结。

## 债务与范围

- `DEBT-CONFLICT-B25-001`：同 layer 同属性时间区间**重叠**仅「检测 + 声明债务」（T05 `detectOverlap` 打印 `[DEBT-CONFLICT-B25-001]` 警告），完整多事件叠加语义留给 Day 27。校验器目前对重叠采取声明而非硬拒（与设计一致）。
- `UX-001`（参数旁「预计效果」提示）：未在 `PresetParameterForm` 实现，推迟至 Day 27 与重叠语义一并处理。
- 旧 `shared/domain` evaluator 保留并标注 `@deprecated`，未删除（避免破坏既有历史测试）。
- T01 回填依赖「存在早于 `timelineEvents` 引入的 v0–v3 文件」；本仓库既有示例均为 v5，回填以稳妥与测试覆盖为准。
- 真实 Electron UI 手动回归（headless 环境无法执行）见 HIGH-001 ENV-LIMITED 说明。

## 决策

- **M3 Gate 结论：FAIL**。自动化闸门（TYPE/LINT/BUILD/UNIT/INTEGRATION）全绿，16 项刀刃表中 15 项代码层面 PASS；但 HIGH-001 真实 GUI 点击回归因 headless 环境限制未执行，按工单「未验证项按 FAIL」原则标记为 ENV-LIMITED 并计入 FAIL。主路径（真人制作镜头 + 应用预设 + 保存重开）未经真实 Electron UI 验证，不得 PASS。
- **冻结 Day 26~45 实际开发**：在真实 Electron 手动验收补齐并通过 M3 Gate 前，不得启动 Day 26~45 实际开发。Day 27 的 `DEBT-CONFLICT-B25-001` 重叠叠加语义与 `UX-001` 亦待真实验收后推进。
