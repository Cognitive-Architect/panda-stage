# Panda Stage 实机小毛病收口台账

> 用途：集中记录当前 main 在真实 Windows / Electron 使用过程里发现的“小毛病、视觉瑕疵、交互不顺手、验收偏差”。
>
> 这不是 Day29 PR #233 的继续施工单，也不是新的大功能路线。这里先记录、分级、收口，避免实机问题散落在聊天、截图和旧 PR 里。

## 基线

- 建立日期：2026-09-03
- 基于 main：`de8d4ddced439a2846af39a503ef38985fc53e29`
- 当前状态：`OPEN TRACKER / HUMAN ACCEPTANCE POLISH`
- 目标：把当前 main 的真人实机残留问题集中记录清楚，再逐项决定是否需要单独 Issue / 实现 PR。

## 范围

### 记录这里

- 真实 Windows / Electron 中可复现的小毛病
- UI 视觉不顺眼、拥挤、错位、裁切、响应式异常
- 文案、状态提示、按钮层级、可发现性问题
- 操作路径和设计预期不一致
- 不一定值得立刻修，但以后容易忘的真人观察

### 不在这里直接施工

- 不继续开发 PR #233
- 不在本 PR 中实现功能修复
- 不做 FLA A-G 二次大改
- 不做 Project Launcher 二次大重构
- 不启动 V2-S / Animate semantic import
- 不借“小毛病”名义混入架构重构

## 记录格式

每发现一个问题，按下面格式追加：

```text
### LM-XXX｜一句话问题名

状态：OPEN / CONFIRMED / FIXED / WONTFIX
严重度：P0 / P1 / P2 / P3
区域：例如 Launcher / Asset Library / FLA / Timeline / Preview / Inspector
实机环境：Windows + Electron

现象：
用户实际看到了什么。

复现：
1. ...
2. ...
3. ...

预期：
应该是什么样。

证据：
截图 / 录屏 / 日志 / 人工描述。

备注：
是否值得单独开 Issue，是否可能和已有 PR / Issue 重叠。
```

## 严重度约定

- `P0`：阻断使用、崩溃、数据风险，发现即停止继续验收
- `P1`：核心路径明显坏掉，但有有限绕路
- `P2`：功能可用，但交互 / 视觉 / 状态明显不对
- `P3`：轻微瑕疵、文案、视觉 polish、低频边角

## 当前问题清单

| ID | 区域 | 严重度 | 状态 | 一句话描述 | 是否需单独 Issue |
|---|---|---:|---|---|---|
| LM-001 | Project Launcher / No Project | P3 | CONFIRMED | “最近项目”空状态卡内容视觉重心偏下，上半部留白过大 | 暂不需要 |
| LM-002 | Main Editor / Top Toolbar | P3 | CONFIRMED | 顶部工具栏常驻“已保存”状态胶囊冗余，占用核心操作区 | 暂不需要 |

## 问题明细

### LM-001｜No Project 的“最近项目”空状态卡视觉重心偏下

状态：`CONFIRMED`  
严重度：`P3`  
区域：`Project Launcher / No Project / Recent Projects empty state`  
实机环境：`Windows + Electron`

**现象：**

在 Project Launcher 的 No Project 状态下，“最近项目”区域的空状态卡片中，
“还没有最近项目 / 新建或打开项目后，会显示在这里。”这组文案整体看起来偏下，
卡片上半部留下了一块较明显、但没有承担信息层级或视觉功能的空白。

结果是卡片的视觉重心不在中部，像内容被向下压了一截；功能不受影响，但真人观看时会产生明显的“不齐 / 有点歪 / 上面空一块”的不舒服感。

**复现：**

1. 启动当前 `main` 的 Windows Electron 应用。
2. 进入 Project Launcher。
3. 保持 No Project 状态，且 Recent Projects 为空。
4. 观察“最近项目”标题下方的 empty-state 卡片。

**预期：**

- empty-state 文案组在卡片中的垂直视觉重心更自然；
- 上下留白应更均衡，或通过更紧凑的卡片高度消除无意义空区；
- 保持当前简洁、低噪音的 Launcher 方向，不通过增加图标、说明文字或额外装饰来“填空”。

**证据：**

- 2026-09-03 维护者真人实机截图：红框标出了 Recent Projects empty-state 区域；
- 人工观察：文案“感觉歪了”，上方空白过大、视觉不舒服。

**建议修复方向（仅供后续实施单使用）：**

优先检查 empty-state 容器自身的 `min-height / padding / align-items / justify-content` 等布局约束，
目标应是“收紧或重新垂直居中”，而不是往空白处继续堆新内容。

**备注：**

- 当前属于纯视觉 polish，不影响 New/Open/Recent Projects 功能和项目状态 truth；
- 暂不值得单独开 Issue，先留在 #416；
- 如果后续出现多个 Launcher spacing / alignment 问题，可一起切成一个小型 Launcher polish implementation slice。

### LM-002｜移除主页顶部工具栏冗余“已保存”状态胶囊

状态：`CONFIRMED`  
严重度：`P3`  
区域：`Main Editor / Top Toolbar / Save state presentation`  
实机环境：`Windows + Electron`

**现象：**

主编辑器顶部工具栏中，在“重做”和真正的“保存”按钮之间，常驻显示一个“已保存”状态胶囊。

该元素本身不承担操作，只是在核心工具栏中重复展示当前保存状态；它与紧邻的“保存”操作形成视觉和语义重复，并占用了本应留给主要操作的横向空间。

真人实机观看时，这个状态胶囊显得多余，工具栏因此更拥挤，也削弱了“撤销 / 重做 / 保存 / 更多”这组主操作的清晰层级。

**复现：**

1. 启动当前 `main` 的 Windows Electron 应用。
2. 打开任意项目并进入主编辑器。
3. 观察顶部工具栏“撤销 / 重做 / 已保存 / 保存 / 更多”区域。
4. 项目处于已保存状态时，“已保存”胶囊持续占据工具栏位置。

**预期：**

- 从主页顶部主工具栏中移除常驻“已保存”状态胶囊；
- 保留真正的“保存”操作按钮；
- 保留底层 saved / dirty 状态 truth 与保存逻辑，不因为删掉展示组件而删除状态判断；
- 若未来确有必要反馈保存结果，应优先使用不长期占位的轻量反馈，而不是重新增加常驻状态块。

**证据：**

- 2026-09-03 维护者真人实机截图：红框标出了顶部工具栏“已保存”状态胶囊；
- 维护者明确要求：删除该多余“已保存”展示。

**建议修复方向（仅供后续实施单使用）：**

仅移除该常驻状态展示组件及相应布局占位，避免误删项目的 dirty/saved 计算、保存按钮 enable/disable 逻辑或保存后的真实状态更新。

**备注：**

- 当前属于 UI 信息层级与空间利用 polish，不是保存功能缺陷；
- 暂不单独开 Issue，先留在 #416；
- 后续若积累更多主页顶部工具栏的视觉问题，可与其一起形成一个小型 toolbar polish implementation slice。

## 收口规则

1. 只记录真实观察，不把“也许会有问题”写成已知缺陷。
2. 每条问题必须能回答：哪里不对、怎么复现、预期是什么。
3. P0/P1 原则上单独开 Issue；P2/P3 可先留在本台账，积累到合适批次再切实施单。
4. 修复完成后必须回到实机复验，不能只凭 CI / 单测改成 FIXED。
5. 如果某条问题其实属于 #233 Day29，则只做交叉引用，不在这里偷偷接手 #233。

## Definition of Done

这个记录 PR 只有在以下条件满足时才考虑关闭：

- 当前 main 已知实机残留问题全部有去向：已修 / 单独 Issue / 明确 WONTFIX；
- 没有未归类的 P0/P1；
- 所有标记 FIXED 的问题都有真人实机复验；
- 不把未验证猜测当事实。

## 当前结论

这个 PR 的角色是“真人验收问题收集箱”，不是实现分支。

先把毛病捡干净，再决定下一刀砍哪里。
